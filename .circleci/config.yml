version: 2.1

orbs:
  maven: circleci/maven@0.0.12

workflows:
  maven_test:
    jobs:
      - maven/test:
          command: 'clean test -Denv=${env}'
    steps:      
      - store_test_results:
          path: ~/test-results
