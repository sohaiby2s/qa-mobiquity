version: 2.1

orbs:
  maven: circleci/maven@0.0.12
jobs:
  build:
    executor:
      name: execution
      tag: '10.4'
    steps:
      - checkout
      - maven/test:
          steps:
            - run: mvn clean
      - run: mvn test
          
